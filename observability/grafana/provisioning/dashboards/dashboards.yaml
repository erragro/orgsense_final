apiVersion: 1

providers:
  - name: kirana-kart
    orgId: 1
    folder: Kirana Kart
    folderUid: kirana-kart
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
